const IP = 'localhost';
const PORT = 50541;
const moveUp = 'Move: up';
const moveLeft = 'Move: left';
const moveDown = 'Move: down';
const moveRight = 'Move: right';

module.exports = {
  IP,
  PORT,
  moveUp,
  moveLeft,
  moveDown,
  moveRight,
};